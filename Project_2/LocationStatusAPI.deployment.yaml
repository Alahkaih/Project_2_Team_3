apiVersion: v1
kind: deployment
metadata:
  name: location-status-api
  labels:
    app: location-status-api
    tier: webserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: location-status-api
  template:
    metadata:
      labels:
        app: location-status-api
        tier: webserver
    spec:
      containers:
        - name: location-status-api
          image: LocationStatusAPI:latest
          imagePullPolicy: Always
---
apiVersion: v1
kind: service
metadata:
  labels:
    app: webserver
  name: webserver
spec:
  ports:
    - port: 90
  selector:
    app: location-status-api
  type: NodePort